source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gemspec

group :test do
  gem 'byebug'
  gem 'fuubar'
  gem 'generator_spec'

  gem "rake"
  gem "rspec"
end
